# Memory Log - June 10, 2026

## Overview

Optimized performance metrics for mobile and desktop, modularized the blog dataset, and created dynamic trilingual single blog pages.

## Key Changes

1. **Dynamic Font Injection**:
   - In [layout.tsx](file:///c:/Users/karim/Desktop/islamteban/app/[locale]/layout.tsx), conditionally injected font class name variables.
   - Arabic pages (`ar`) only load/inject `--font-amiri-next`, `--font-noto-arabic-next`, and `--font-cairo-next`.
   - English/French pages (`en`/`fr`) only load/inject `--font-cormorant-next`, `--font-lora-next`, and `--font-dm-sans-next`.
   - This prevents the browser from downloading unused font families, resolving critical request chain issues.
2. **Font Weight Pruning**:
   - Pruned Google Font weights across all 6 families in [layout.tsx](file:///c:/Users/karim/Desktop/islamteban/app/[locale]/layout.tsx) (e.g. removed Cormorant Garamond 300, 400, 500, Cairo 300, etc.) to cut font payload size by over 50%.
3. **Blog Dataset Extraction**:
   - Moved trilingual articles dataset `ARTICLES_DATA` and `BASE_ARTICLES` configuration out of `page.tsx` and into [data.ts](file:///c:/Users/karim/Desktop/islamteban/app/[locale]/blog/data.ts) for clean reuse.
4. **Scholarly Single Article Dynamic Route**:
   - Implemented [page.tsx](file:///c:/Users/karim/Desktop/islamteban/app/[locale]/blog/[slug]/page.tsx) with SSG (`generateStaticParams`).
   - Standardized readability styles with drop caps (for Latin), pull quotes with gold borders, dynamic Al-Azhar authorship markers, bibliographic references cards, and dynamic related articles.
   - Refactored the blog listing page to route directly to these dynamic pages, slimming down its JS payload weight.
5. **Ultra-Premium Single Course Page Redesign (Phase 3)**:
   - Redesigned the dynamic course detail page at [page.tsx](file:///c:/Users/karim/Desktop/islamteban/app/[locale]/programs/[slug]/page.tsx).
   - Added a dark navy manuscript hero with gold-accent framing, metadata grids, and animated golden elements.
   - Implemented scholarly drop-caps for Arabic/Latin content, two-column custom-numbered syllabus cards, a visual connected study timeline roadmap (dark-themed), and a validated traditional certification mock-up.
   - Fixed translation apostrophes and resolved Turbopack syntax compilation issues by using double quotes.
6. **Program Category Routing Redirects (Phase 3)**:
   - Changed dropdown paths in [Header.tsx](file:///c:/Users/karim/Desktop/islamteban/components/layout/Header.tsx) and showcase cards in [ProgramsShowcase.tsx](file:///c:/Users/karim/Desktop/islamteban/components/sections/ProgramsShowcase.tsx) to point to `/programs?track=...` (e.g. `?track=quran`).
   - Replaced old static track pages (quran, arabic, islamic) with Next.js server-side `redirect` handlers to route users directly to catalog tabs, ensuring backward-compatibility.

## Verification & Deployment

- Production compilation (`npm run build`) succeeded with 0 errors. All 24 dynamic locale-slug path combinations pre-rendered statically.
- Verification script tests (`node verify.js`) all passed.
- Pushed commit to remote repository `main` branch.

7. **Multi-Language Switcher Routing Fix**:
   - Re-architected the `switchLocale` function in [Header.tsx](file:///c:/Users/karim/Desktop/islamteban/components/layout/Header.tsx) and [Footer.tsx](file:///c:/Users/karim/Desktop/islamteban/components/layout/Footer.tsx).
   - Resolved the bug where transitioning from the default locale (English, which has no `/en/` prefix under `localePrefix: 'as-needed'`) would strip directories from the path (e.g. `/programs/quran-tajweed` crashed by redirecting to `/fr/quran-tajweed`).
   - Implemented dynamic prefix checking that correctly swaps or appends/splices language prefixes based on the active path segments.
   - Preserved search query parameters (such as `?track=arabic`) across language switches using client-side `window.location.search`, preventing Next.js layout static de-optimization.
   - Pushed commit `73edf57` to the remote repository.

8. **Related Courses Card Redesign**:
   - Updated the related courses list block in [page.tsx](file:///c:/Users/karim/Desktop/islamteban/app/[locale]/programs/[slug]/page.tsx).
   - Replaced plain text blocks with the full-fidelity featured/catalog course cards, featuring cover images, path badges, dynamic Lucide icon mappings, tagline descriptors, and full Syllabus stat grids (duration, syllabus manual, level).
   - Verified that typescript validation checks and production build succeeded without issues.
   - Pushed commit `7b68fe5` to the remote repository.

9. **English Switching Cookie Sync Fix**:
   - Updated the `switchLocale` logic in both [Header.tsx](file:///c:/Users/karim/Desktop/islamteban/components/layout/Header.tsx) and [Footer.tsx](file:///c:/Users/karim/Desktop/islamteban/components/layout/Footer.tsx) to explicitly write the `NEXT_LOCALE` cookie via `document.cookie` before routing.
   - This prevents the Next-intl middleware from falling back to the previous language cookie (e.g. keeping the user stuck on `/fr` or `/ar` dynamic layouts) when they request to return to the default locale prefixless route.
   - Pushed commit `b33052d` to the remote repository.

10. **Social Media Footer Integration**:
    - Added the academy's verified social media profiles (Facebook, YouTube, Instagram, X/Twitter, and Telegram) to the brand column in [Footer.tsx](file:///c:/Users/karim/Desktop/islamteban/components/layout/Footer.tsx).
    - Modeled the visual icon system and alignment to mirror [page.tsx](file:///c:/Users/karim/Desktop/islamteban/app/[locale]/contact/page.tsx) with custom micro-animations and gold-accent borders.
    - Verified compilation and pushed commit `c559bb5` to the remote repository.

11. **Telegram Icon & Favicon Re-alignment**:
    - Replaced the temporary Lucide `Send` paperplane icon with the official circular Telegram logo SVG path in [Footer.tsx](file:///c:/Users/karim/Desktop/islamteban/components/layout/Footer.tsx) to ensure pixel-perfect consistency across all footer social icons.
    - Wrote a custom Python script using `PIL/Pillow` to read the high-resolution, transparent, square logo `public/logo-new.webp`, and render it as multi-resolution `.ico` formats (16x16, 32x32, 48x48, 256x256) and a 256x256 `.webp` fallback.
    - Replaced the default Vercel triangle favicon at [favicon.ico](file:///c:/Users/karim/Desktop/islamteban/app/favicon.ico) and generated [app-icon.webp](file:///c:/Users/karim/Desktop/islamteban/public/app-icon.webp) which was already mapped inside layout metadata.
    - Verified compilation and pushed commit `46134f6` to the remote repository.

12. **Homepage Articles Redirection Links Fix**:
    - Replaced the placeholder `href="#"` values in [LatestArticles.tsx](file:///c:/Users/karim/Desktop/islamteban/components/sections/LatestArticles.tsx) (for article titles, "Read Full Article" links, and the "View Academic Blog" CTA button) with dynamic, locale-prefixed URLs: `/${locale}/blog/${article.slug}` and `/${locale}/blog`.
    - Pushed commit `e7fccf7` to the remote repository.


